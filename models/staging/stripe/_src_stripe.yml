sources:
  - name: stripe
    database: jaffle-shop-474413  
    schema: dbt_sellingson_jaffle_shop  
    tables:
      - name: payments  # The name you'll use to reference this source
        identifier: raw_payments # The actual name of the table in the source database
        config:
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 24
              period: hour
          loaded_at_field: _batched_at